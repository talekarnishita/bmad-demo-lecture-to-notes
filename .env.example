# Dev demo — Option B (minimal web app)
# Copy to .env and set your keys.

OPENAI_API_KEY=sk-...

# Optional: max upload size in bytes (default 25_000_000 for Whisper limit)
# MAX_UPLOAD_BYTES=25000000
